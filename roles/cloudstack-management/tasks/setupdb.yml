- name: Setup CloudStack databases
  command: >
    /usr/bin/cloudstack-setup-databases
    cloud:{{ mysql_cloud_password }}@new-bd
    --deploy-as=root:{{ mysql_root_password }}
    --force
- name: Setup CloudStack manager
  command: /usr/bin/cloudstack-setup-management

